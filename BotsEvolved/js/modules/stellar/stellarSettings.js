/**
 * @author Kate Compton
 */

define([], function() {

    // Modifiable settings
    var settings = {
        modeSwitchDistances : {
            up : [20, 60],
            down : [19, 59],
        }
    };

    return settings;
});
