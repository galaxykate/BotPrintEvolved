/**
 * @author Kate Compton
 */

define(["modules/evoSim/evoPopulation", "common"], function(EvoPopulation, COMMON) {
    // Create a world for the particles to be in

    var ParticleWorld = Class.extend({
        init : function() {

        },
    });
});
